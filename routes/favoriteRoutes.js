const { Router } = require("express");

const Product = require("../models/products.model");

const route = Router();

//ver com o DK/Gabs como fazer. Precisa de referencia em produto? ou um modelo novo?

module.exports = route;